<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>เงินเดือน</title>
    <link rel="icon" type="image" href="/image/logo_web.png">
    <link href="/css/styles.css" rel="stylesheet" />
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>

    <!--font-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Prompt&display=swap" rel="stylesheet">

    </script>
</head>

<body>
    <header class="mt-5 sticky-top" style="background-color: white;">
        <div class="container">
            <br>
            <div class="row">
                <div class="col-2 ">
                    <script>
                        console.log("Image path:", "/icon/back_icon.svg");
                    </script>
                    <a href="/user_menu"> <img src="/icon/back_icon.svg" alt="Back"></a>
                </div>
                <div class="col-8 text-center">
                    <h2>เงินเดือน</h2>
                </div>
            </div>
        </div>
    </header>

    </header>

    <div class="container">
        <div class="text-end">
            <p class="mb-0">อัตราค่าจ้างต่อชั่วโมง: <%= hourlyRate %>
            </p>
        </div>
    </div>

    <main class="container mt-4">
        <% if (receipts && receipts.length> 0) { %>
            <% receipts.sort((a, b)=> new Date(b.slip_month) - new Date(a.slip_month)).forEach(receipt => { %>
                <div class="card mb-3">
                    <div class="card-body">
                        <h5 class="card-title text-center">เงินเดือน
                            <% let displayDate='ไม่ระบุวันที่' ; if (receipt.day_slip) { const date=new
                                Date(receipt.day_slip); if (!isNaN(date.getTime())) {
                                displayDate=date.toLocaleString('th-TH', { month: 'long' , year: 'numeric' }); } } %>
                                <%= displayDate %>
                        </h5>
                        <p class="card-text">จำนวนชั่วโมงทำงาน: <%= receipt.hr_month %> ชั่วโมง</p>
                        <p class="card-text">จำนวนเงิน: <%= receipt.money_month.toLocaleString('th-TH', {
                                style: 'currency' , currency: 'THB' }) %>
                        </p>
                        <% if (receipt.s_pic) { %>
                            <img src="data:image/jpeg;base64,<%= receipt.s_pic.toString('base64') %>" alt="Slip"
                                class="img-fluid mx-auto d-block" style="max-width: 65%; height: auto;">
                            <% } else { %>
                                <p>ไม่มีรูปสลิปเงินเดือน</p>
                                <% } %>
                    </div>
                </div>
                <% }); %>
                    <% } else { %>
                        <div class="alert alert-info">ไม่พบข้อมูลเงินเดือน</div>
                        <% } %>
    </main>

</body>

<!-- end Content -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"
    crossorigin="anonymous"></script>
<script src="js/scripts.js"></script>

</body>

</html>